<div class="top_requests">
  <div class="mytable" style="width:350px;">
	  <table >
		  <tr>
			  <td>
				  Top Requests
			  </td>
		  </tr>
  <% @songs.each do |song| %>
    <% unless song.status == 1 %>
		  <tr>
		    <td>
		    <%= song.song_title %><br/>
        (<%= pluralize song.reputation_for(:votes).to_i, "vote" %>) - (Req: <%= song.requester %>)
        </td>
		  </tr>
    <% end %>
  <% end %>
    </table>
  </div>
</div>
